---
export interface image {
    src: string;
    alt: string;
}
const { src, alt } = Astro.props;
---
<figure class="group py-5 [break-inside:avoid]">
    <img class="rounded-md w-full h-80 object-cover object-top transition-all duration-500 group-hover:rounded-xl"
         src={src}
         alt={alt}>
</figure>
